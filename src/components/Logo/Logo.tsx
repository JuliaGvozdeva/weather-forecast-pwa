import React from 'react';
import logo from '../../assets/images/logo.png';

const Logo: React.FC = () => (
  <div className="logo">
    <img src={logo} alt="logo-img" />
  </div>
);

export default Logo;
